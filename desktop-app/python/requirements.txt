# Tesseract OCR - Ultra-lightweight version
# Chỉ cần 2 packages nhẹ, không cần PyTorch hay deep learning models

pytesseract>=0.3.10
Pillow>=10.0.0

# HTTP requests for Cloud OCR
requests>=2.31.0

# PDF merging for batch scanner
pypdf>=3.0.0

# PDF to image conversion - Using PyMuPDF (NO Poppler required!)
PyMuPDF>=1.23.0
# PyMuPDF includes all binary dependencies, no external tools needed

# VietOCR - Optional Vietnamese OCR Engine (Transformer-based)
# Chỉ cài nếu cần độ chính xác cao hơn cho tiếng Việt (90-95%)
# Uncomment dòng dưới để cài:
# vietocr>=0.3.9
# torch>=2.0.0
# torchvision>=0.15.0

# EasyOCR - Optional Vietnamese OCR Engine (Recommended)
# Cân bằng giữa accuracy (90-92%) và ease of use
# Tốc độ: ~10-11s/page với optimization
# Uncomment dòng dưới để cài:
# easyocr>=1.7.0
# torch>=2.0.0
# torchvision>=0.15.0

# Cloud OCR - Google Cloud Vision & Azure Computer Vision
# Requires API keys (BYOK - Bring Your Own Key)
# Install: pip install requests (already included above)
# Google: 1,000 free requests/month, $1.50/1K after
# Azure: 5,000 free requests/month, $1.00/1K after

# Lưu ý: Cần cài Tesseract binary riêng
# Windows: https://github.com/UB-Mannheim/tesseract/wiki
# Mac: brew install tesseract tesseract-lang
# Linux: sudo apt-get install tesseract-ocr tesseract-ocr-vie
# 
# Sau khi cài, Tesseract sẽ tự động detect
